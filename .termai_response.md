### Modified Files

Since you requested adding movement modules to ArenixClient Fabric 1.21.4, I have analyzed the provided codebase and found the necessary modules (`Speed.java`, `Sprint.java`, `BunnyHop.java`, `NoFall.java`, `Fly.java`) already exist under the package `com.arenix.client.module.impl.movement`. However, to make them functional and integrate them correctly with `ArenixClient`, modifications are needed in the `ModuleManager` class. Below are the modified files that need to be created or modified:

```java
// src/main/java/com/arenix/module/ModuleManager.java
package com.arenix.module;

import com.arenix.client.module.impl.combat.ExampleCombatModule;
import com.arenix.client.module.impl.movement.Speed;
import com.arenix.client.module.impl.movement.Sprint;
import com.arenix.client.module.impl.movement.BunnyHop;
import com.arenix.client.module.impl.movement.NoFall;
import com.arenix.client.module.impl.movement.Fly;

import java.util.ArrayList;
import java.util.List;

public class ModuleManager {

    private final List<Module> modules = new ArrayList<>();

    public ModuleManager() {
        // Register your modules here
        registerModule(new Speed());
        registerModule(new Sprint());
        registerModule(new BunnyHop());
        registerModule(new NoFall());
        registerModule(new Fly());
        // Add more modules as you create them
    }

    private void registerModule(Module module) {
        modules.add(module);
    }

    // ... rest of the class remains the same ...
}
```

```java
// src/main/java/com/arenix/client/ArenixClient.java
package com.arenix.client;

import com.arenix.client.event.EventBus;
import com.arenix.client.module.ModuleManager;
import net.fabricmc.api.ClientModInitializer;
import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
import net.minecraft.client.MinecraftClient;
import net.minecraft.client.option.KeyBinding;
import net.minecraft.client.util.InputUtil;
import org.lwjgl.glfw.GLFW;

public class ArenixClient implements ClientModInitializer {

    public static final String MOD_ID = "arenix";
    public static ArenixClient INSTANCE;

    private KeyBinding moduleKeybind;
    private ModuleManager moduleManager;
    private EventBus eventBus;

    @Override
    public void onInitializeClient() {
        INSTANCE = this;

        moduleManager = new ModuleManager();
        eventBus = new EventBus();

        moduleKeybind = KeyBindingHelper.registerKeyBinding(new KeyBinding(
                "key.arenix.toggle_menu", // The translation key of the keybinding
                InputUtil.Type.KEYSYM,
                GLFW.GLFW_KEY_RIGHT_SHIFT, // The default key
                "category.arenix.main" // The translation key of the keybinding's category
        ));

        ClientTickEvents.END_CLIENT_TICK.register(client -> {
            while (moduleKeybind.wasPressed()) {
                moduleManager.getModules().forEach(Module::toggle);
                moduleKeybind.setPressed(false);
            }
        });

        // Also, ensure the module manager is ticked every frame
        ClientTickEvents.END_CLIENT_TICK.register(client -> {
            moduleManager.onClientTick();
        });

        System.out.println("Arenix Client Initialized!");
    }

    // ... rest of the class remains the same ...
}
```

### Explanation:

- **ModuleManager Class Modification:** The `Speed`, `Sprint`, `BunnyHop`, `NoFall`, and `Fly` modules are now registered in the `ModuleManager` constructor. This ensures these modules are initialized and managed by the mod.
- **ArenixClient Class Modification:** In the `ArenixClient` class, the `ModuleManager` instance is created, and a client tick event is registered to call `onClientTick()` on the `ModuleManager`. This ensures that the `onTick()` method of each module is called every frame, making the modules active and functional.

With these modifications, the movement modules (`Speed.java`, `Sprint.java`, `BunnyHop.java`, `NoFall.java`, `Fly.java`) should be integrated into the ArenixClient Fabric mod and be functional.